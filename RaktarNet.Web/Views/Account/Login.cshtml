@model LoginViewModel

<div class="login-page">
    <div class="login-card">
        <div class="login-left">
            <h1>RaktarNet</h1>
            <p>Raktárkészlet-nyilvántartó rendszer</p>
        </div>

        <div class="login-right">
            <h2>Bejelentkezés</h2>
            <p class="muted">Kérjük, adja meg felhasználónevét és jelszavát.</p>

            @if (!string.IsNullOrWhiteSpace(Model.Error))
            {
                <div class="error-box">@Model.Error</div>
            }

            <form method="post">
                <label>Felhasználónév</label>
                <input asp-for="Username" placeholder="Felhasználónév" />

                <label>Jelszó</label>
                <input asp-for="Password" type="password" placeholder="Jelszó" />

                <button type="submit">🔐 Belépés</button>
            </form>

            <p class="muted demo">Csak jogosult felhasználók számára elérhető.</p>
        </div>
    </div>
</div>
